site :opscode

# TODO:
# We need to either:
#   - some sort of environments / version constraints [yak shave]
#   - Find the omnibus package for a chef 10 server
cookbook 'chef-server'
cookbook 'chef-server', git: 'git://github.com/opscode-cookbooks/chef-server.git'
